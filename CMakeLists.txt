cmake_minimum_required(VERSION 3.16)  //Minimal Version.  You can change it for your version, which you can watch with "cmake --version"

project (lesson1)  //name of directory where is your .cpp file

add_executable(hello hello.cpp) //name of executable file
